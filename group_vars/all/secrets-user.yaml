---
# Dedicated secrets service account identity on target hosts — referenced by roles
# (e.g. secrets_setup) that configure file permissions, group access, and SSH keys.

secrets_user: secrets
secrets_group: secrets
