---
# Admin user override for specific server connections
# Overrides vault settings when admin user access is required

# Connect as admin user instead of vault default
ansible_user: admin
ansible_become_password: "{{ admin_password }}"  # Use the admin user's password for privilege escalation
