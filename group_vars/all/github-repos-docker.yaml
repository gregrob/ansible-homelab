---
# github_clone role variables

# Baseline repositories deployed to all Docker hosts
common_github_repos:
  - name: docker-homelab
    dest: /srv/docker-homelab

# Merged list: Combines baseline with host-specific additions (defined in host_vars)
github_repos: "{{ common_github_repos + (host_github_repos | default([])) }}"
