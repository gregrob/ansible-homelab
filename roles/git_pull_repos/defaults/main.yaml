---
# Default variables for git_pull_repos role

# Base folder containing Git repositories
git_base_folder: "/srv"

# Default branch to pull (usually main or master)
git_default_branch: "HEAD"

# User who owns the Git repositories
git_user: "admin"

# Whether to show verbose output
git_verbose: true
