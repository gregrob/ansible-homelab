---
# This file is part of the Ansible inventory.

all:
  children:

    configure_target:
      hosts:
        #nut-001.trusted.max.lan: {}
        {}

    ansible:
      hosts:
        ansible-001.containers.max.lan: {}

    docker_privileged:
      hosts:
        frigate-001.containers.max.lan: {}
        jellyfin-001.containers.max.lan: {}
        development-002.containers.max.lan: {}
        ollama-001.containers.max.lan: {}

    docker_unprivileged:
      hosts:
        3dprinter-001.containers.max.lan: {}
        infisical-001.trusted.max.lan: {}

    vms:
      hosts:
        download-001.containers.max.lan: {}  
        dns-001.dns.max-lan.com:
           ansible_host: dns-001.dns.max-lan.com
        dns-002.dns.max-lan.com:
           ansible_host: dns-002.dns.max-lan.com

    test_target:
      hosts:
        {}

    secrets:
      hosts:
        secrets.max.lan: {}
        
    local:
      hosts:
        localhost:
          ansible_connection: local

    lxcs:
      children:
        ansible:
        docker_privileged:
        docker_unprivileged:
    
    git_hosts:
      children:
        ansible:
        docker_privileged:
        docker_unprivileged:
        vms:
    
    update_hosts:
      children:
        ansible:
        docker_privileged:
        docker_unprivileged:
        vms:
